# T19 — i18n completion (en + fr)

**Milestone**: M4 · **Deps**: T18 · **Agent**: sonnet / medium effort / no skills

## Goal
Complete bilingual catalogs; zero hardcoded UI strings.

## References
- `docs/ARCHITECTURE.md` — "i18n". French room-type names seeded from old `src/scripts/utils.js` `getRoomTypeString` (Salon, Chambre, Cuisine, Bureau, Salle à manger, Cave, Divers, Extérieur, Salle de bain, Couloir, Garage — add proper accents, the old code lacked them).

## Files
`app/src/i18n/{en,fr}.json` completed; sweep every view/component for stray literals.

## Acceptance criteria
- [ ] Grep audit shows no user-facing hardcoded strings in templates (document the grep used and its exceptions — units like °C, the Calaos logo text).
- [ ] `fr.json` and `en.json` cover the same key set (spec compares key trees); roomType.* complete in both.
- [ ] Playwright fr-locale project green: login screen and home render French; room type labels French.
- [ ] Server-provided room/IO names remain untranslated (spec).
- [ ] `npm run test:unit`, `test:e2e`, `lint`, `typecheck` green.
