node_modules
.serena/

# local env overrides (.env.development.local etc.) — never committed
*.local

# build output — never committed; release.yml builds and publishes it
dist/

# test/build tooling output
test-results/
playwright-report/
*.tsbuildinfo
